<section class="{{styles.auth}}">
    <form id="dynamic-form" class="{{styles.form}} {{formModifierClass}}">
      <h2 class="{{styles.title}}">{{title}}</h2>
      {{{ fields }}}

      {{{ button }}}
  
      {{{ link }}}
    </form>
</section>